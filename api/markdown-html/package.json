{
  "private": true,
  "name": "api-markdown-html",
  "description": "Run zenn-markdown-html on Cloud functions.",
  "version": "0.1.1",
  "engines": {
    "node": "10"
  },
  "scripts": {
    "build": "tsc -p .",
    "deploy:functions": "./deploy-functions.sh",
    "deploy": "npm run build && npm run deploy:functions"
  },
  "dependencies": {
    "zenn-markdown-html": "latest"
  },
  "devDependencies": {
    "@types/node": "^14.0.13",
    "typescript": "^3.9.5"
  }
}
